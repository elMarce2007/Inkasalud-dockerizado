apiVersion: v1
kind: Service
metadata:
  name: ventas-service
  namespace: inkasalud
  labels:
    app: ventas-service
    app.kubernetes.io/part-of: inkasalud-system
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
      name: http
  selector:
    app: ventas-service
