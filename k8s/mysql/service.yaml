apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: inkasalud
  labels:
    app: mysql
    app.kubernetes.io/part-of: inkasalud-system
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
  selector:
    app: mysql
