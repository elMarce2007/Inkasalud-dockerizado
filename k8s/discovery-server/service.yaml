apiVersion: v1
kind: Service
metadata:
  name: discovery-server
  namespace: inkasalud
  labels:
    app: discovery-server
    app.kubernetes.io/part-of: inkasalud-system
spec:
  type: ClusterIP
  ports:
    - port: 8761
      targetPort: 8761
      protocol: TCP
      name: http
  selector:
    app: discovery-server
