apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: inkasalud
  labels:
    app: auth-service
    app.kubernetes.io/part-of: inkasalud-system
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: auth-service
