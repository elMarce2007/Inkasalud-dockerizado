apiVersion: v1
kind: Service
metadata:
  name: config-server
  namespace: inkasalud
  labels:
    app: config-server
    app.kubernetes.io/part-of: inkasalud-system
spec:
  type: ClusterIP
  ports:
    - port: 8888
      targetPort: 8888
      protocol: TCP
      name: http
  selector:
    app: config-server
