apiVersion: v1
kind: Service
metadata:
  name: personal-service
  namespace: inkasalud
  labels:
    app: personal-service
    app.kubernetes.io/part-of: inkasalud-system
spec:
  type: ClusterIP
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
      name: http
  selector:
    app: personal-service
